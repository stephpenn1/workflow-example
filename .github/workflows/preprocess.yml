on:
  workflow_dispatch:

name: Pre-process TEMPEST data

jobs:
  timed_action:
    name: Pre-process TEMPEST data
    runs-on: ubuntu-latest
    container:
      image: rocker/tidyverse:4.2
    permissions:
      contents: write
    
    steps:
      - name: Hello
        run: echo "Hello world!"
